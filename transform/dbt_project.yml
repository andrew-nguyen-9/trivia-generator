name: parlor
version: "1.0.0"
profile: parlor

model-paths: ["models"]
target-path: target
clean-targets: [target, dbt_packages]

vars:
  # bronze layer location, relative to this dbt project dir
  raw_glob: "../data/raw/*.jsonl"

models:
  parlor:
    staging:
      +materialized: view
    marts:
      +materialized: table
